export enum Status {
	DONE = 'done', // all values are present, in the correct location
	TRANSIT = 'transit', // all values are present, wrong location
	INCOMPLETE = 'incomplete' // not all values are present
}
